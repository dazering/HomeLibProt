module HomeLibProt.CollectionManager.SqlDumps.Flibusta

let authors = "lib.libavtorname.sql.gz"

let authorships = "lib.libavtor.sql.gz"

let bookGenres = "lib.libgenre.sql.gz"

let books = "lib.libbook.sql.gz"

let bookSeries = "lib.libseq.sql.gz"

let genres = "lib.libgenrelist.sql.gz"

let series = "lib.libseqname.sql.gz"

let rates = "lib.librate.sql.gz"
