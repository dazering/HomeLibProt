module HomeLibProt.CollectionManager.SqlDumps.Librusec

let authors = "libavtors.sql.gz"

let authorships = "libavtor.sql.gz"

let bookGenres = "libgenre.sql.gz"

let books = "libbook.sql.gz"

let bookSeries = "libseq.sql.gz"

let genres = "libgenres.sql.gz"

let series = "libseqs.sql.gz"
